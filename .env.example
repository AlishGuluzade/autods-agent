# Copy to .env in the repo root before running `docker-compose up`.
# Leave GROQ_API_KEY blank to run the agent with heuristic (rule-based)
# planning/explanations instead of an LLM -- everything still works end to end.
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-70b-versatile
